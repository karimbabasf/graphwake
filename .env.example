# Optional. Set on a public deployment to gate the AI routes behind an access
# token (at least 24 characters). Leave blank for local use.
GRAPHWAKE_API_TOKEN=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
